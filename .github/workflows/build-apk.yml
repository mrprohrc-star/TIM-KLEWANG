name: Build APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'

      - name: Create Clean Project & Compile APK
        run: |
          # 1. Simpan kode dan konfigurasi dependensi
          mkdir -p /tmp/backup_code
          cp -r lib /tmp/backup_code/
          cp pubspec.yaml /tmp/backup_code/
          
          # 2. Buat proyek Flutter baru yang lengkap
          cd /tmp
          flutter create --org com.hondasales.app --project-name honda_sales_app my_app
          
          # 3. Masukkan kode dan dependensi ke proyek baru
          cp -r /tmp/backup_code/lib/* /tmp/my_app/lib/
          cp /tmp/backup_code/pubspec.yaml /tmp/my_app/pubspec.yaml
          
          # 4. Ambil semua library dan compile APK
          cd /tmp/my_app
          flutter pub get
          flutter build apk --debug

          # 5. Salin APK hasil build ke workspace
          mkdir -p $GITHUB_WORKSPACE/output_apk
          cp build/app/outputs/flutter-apk/app-debug.apk $GITHUB_WORKSPACE/output_apk/Honda-Sales.apk

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: Honda-Sales-App
          path: output_apk/Honda-Sales.apk
